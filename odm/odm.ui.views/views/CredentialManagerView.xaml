<Window x:Class="odm.ui.views.CredentialManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:l="clr-namespace:odm.ui.controls"
        Title="Manage Credentials"
        Width="440" Height="360"
        MinWidth="340" MinHeight="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar: Add / Move Up / Move Down — top right -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,4">
            <Button x:Name="btAdd" Width="28" Height="24" Margin="0,0,4,0" ToolTip="Add credential">
                <Image Source="/odm.ui.views;component/images/square-plus.png" Width="16" Height="16" Stretch="Uniform"/>
            </Button>
            <Button x:Name="btMoveUp" Width="28" Height="24" Margin="0,0,4,0" ToolTip="Move Up">
                <Image Source="/odm.ui.views;component/images/square-arrow-up.png" Width="16" Height="16" Stretch="Uniform"/>
            </Button>
            <Button x:Name="btMoveDown" Width="28" Height="24" ToolTip="Move Down">
                <Image Source="/odm.ui.views;component/images/square-arrow-down.png" Width="16" Height="16" Stretch="Uniform"/>
            </Button>
        </StackPanel>

        <!-- Credential grid -->
        <DataGrid x:Name="credGrid" Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  CanUserResizeRows="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  HeadersVisibility="Column"
                  KeyDown="CredGrid_KeyDown"
                  PreviewKeyDown="CredGrid_PreviewKeyDown">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Width="*"
                    Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTemplateColumn Header="Password" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock VerticalAlignment="Center" Padding="2,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="&#x2022;&#x2022;&#x2022;&#x2022;&#x2022;&#x2022;"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Password}" Value="">
                                                <Setter Property="Text" Value=""/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <l:TogglePasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Width="30" Header="">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Background="Transparent" BorderThickness="0"
                                    VerticalAlignment="Center" HorizontalAlignment="Center"
                                    Width="20" Height="20" Padding="2"
                                    Click="BtDeleteRow_Click">
                                <Image Source="/odm.ui.views;component/images/Delete16.png"
                                       Width="14" Height="14" Stretch="Uniform"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bottom: Apply + Cancel -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
            <Button x:Name="btApply"  Content="Apply"  Width="80" Margin="0,0,4,0" Click="BtApply_Click"/>
            <Button x:Name="btCancel" Content="Cancel" Width="80" Click="BtCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
