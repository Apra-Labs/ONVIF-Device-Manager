<UserControl x:Class="odm.ui.controls.TogglePasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="root">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <PasswordBox x:Name="passwordBox" Grid.Column="0"/>
        <TextBox x:Name="textBox" Grid.Column="0" Visibility="Collapsed"/>
        <ToggleButton x:Name="toggleBtn" Grid.Column="1"
                      Width="24" Padding="2,1" Margin="2,0,0,0"
                      VerticalAlignment="Stretch"
                      Focusable="False" IsTabStop="False"
                      ToolTip="Show/hide password">
            <ToggleButton.Style>
                <Style TargetType="ToggleButton">
                    <Setter Property="Content">
                        <Setter.Value>
                            <Image Source="/odm.ui.app;component/Resources/Images/eye.png" Width="16" Height="16" Stretch="Uniform"/>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <Image Source="/odm.ui.app;component/Resources/Images/eye-off.png" Width="16" Height="16" Stretch="Uniform"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ToggleButton.Style>
        </ToggleButton>
    </Grid>
</UserControl>
